# UAT environment configuration
BASE_URL=http://74.225.253.78:8800/UCCDEV/
API_BASE_URL=http://74.225.253.78:8085

# Valid credentials for the positive login test.
# Leave blank to have that test skip itself instead of failing on placeholder data.
LOGIN_EMAIL=admin@amzbizsol.in
LOGIN_PASSWORD=Amazin@123

# Static app-level Basic Auth credentials the frontend sends on every API
# call (observed on the login request). Not a per-user login.
API_BASIC_AUTH_USER=Amazin
API_BASIC_AUTH_PASSWORD=TE@M-W@RK

# Role-based UAT accounts, used by the data-driven login tests
# (utils/data/login-data.csv references these via {{ROLE_EMAIL}} tokens).
SUPER_ADMIN_EMAIL=admin@amzbizsol.in
SUPER_ADMIN_PASSWORD=Amazin@123

CUSTOMER_EMAIL=customer@amzbizsol.in
CUSTOMER_PASSWORD=Amazin@123

SUPPLIER_EMAIL=supplier@amzbizsol.in
SUPPLIER_PASSWORD=Amazin@123

LOGISTIC_EMAIL=logistics@amzbizsol.in
LOGISTIC_PASSWORD=Amazin@123

TRANSPORTER_EMAIL=transporter@amzbizsol.in
TRANSPORTER_PASSWORD=Amazin@123

# Admin role credentials not provided yet — leave blank, that test row will skip
ADMIN_EMAIL=
ADMIN_PASSWORD=


#npx playwright test --grep @smoke
#npx playwright test --grep @regression
#npm run test:smoke
#npm run test:regression

#npm run test:headed -- --grep "@smoke"
#npx cross-env ENV=uat playwright test --grep "@smoke" --headed
